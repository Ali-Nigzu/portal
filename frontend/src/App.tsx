import React, { useState, useEffect } from 'react'; import { BrowserRouter as Router, Routes, Route, Navigate } from 'react-router-dom'; import VRMLayout from './components/VRMLayout'; import EventLogsPage from './pages/EventLogsPage'; import AlarmLogsPage from './pages/AlarmLogsPage'; import DeviceListPage from './pages/DeviceListPage'; import ReportsPage from './pages/ReportsPage'; import AnalyticsComingSoonPage from './pages/AnalyticsComingSoonPage'; import AdminPage from './pages/AdminPage'; import LandingPage from './pages/LandingPage'; import './styles/VRMTheme.css'; import { GlobalControlsProvider } from './context/GlobalControlsContext'; import DashboardV2Page from './dashboard/v2/pages/DashboardV2Page'; import { Credentials } from './types/credentials'; import { deriveOrgIdFromTableName, determineOrgId } from './utils/org'; import { companyLogoDataUri } from './assets/companyLogo'; const Login: React.FC<{onLogin: (credentials: Credentials) => void}> = ({ onLogin }) => { const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false); const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setError(null); try { const response = await fetch('/api/login', { method: 'POST', headers: { 'Content-Type': 'application/json', }, body: JSON.stringify({ username, password }), }); if (response.ok) { const data = await response.json(); const orgFromResponse = data?.user?.orgId ?? data?.user?.org_id ?? deriveOrgIdFromTableName(data?.user?.table_name); onLogin({ username, password, orgId: orgFromResponse ?? determineOrgId({ username }), }); } else if (response.status === 401) { setError('Invalid username or password'); } else { setError('Connection error. Please try again.'); } } catch (err) { setError('Unable to connect to server'); } finally { setLoading(false); } }; return ( <div className="vrm-auth-shell"><div className="vrm-auth-card" role="dialog" aria-labelledby="camOS-login-title"><div><div className="vrm-auth-logo"><img src={companyLogoDataUri} alt="Company Logo" /></div><h2 id="camOS-login-title" className="vrm-auth-title"> camOS </h2><p className="vrm-auth-subtitle">Sign in to monitor your sites</p></div><form onSubmit={handleSubmit} className="vrm-auth-form"><div className="vrm-field"><label className="vrm-label" htmlFor="login-username"> Username </label><input id="login-username" className="vrm-input" type="text" value={username} onChange={(e) => setUsername(e.target.value)} placeholder="Enter username" autoComplete="username" required /></div><div className="vrm-field"><label className="vrm-label" htmlFor="login-password"> Password </label><input id="login-password" className="vrm-input" type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter password" autoComplete="current-password" required /></div> {error && ( <div className="vrm-status vrm-status-warning vrm-auth-error" role="alert" aria-live="assertive"> {error} </div> )} <div className="vrm-auth-actions"><button type="submit" className="vrm-btn vrm-btn-primary" style={{ width: '100%' }} disabled={loading}> {loading ? 'Signing in…' : 'Login'} </button></div></form><div className="vrm-auth-hint"><strong>Demo credentials</strong><br /> Client: client1 / client123 <br /> Admin: admin / admin123 </div></div></div> ); }; const App: React.FC = () => { const [isLoggedIn, setIsLoggedIn] = useState(false); const [credentials, setCredentials] = useState<Credentials>({ username: '', password: '' }); const [userRole, setUserRole] = useState<'client' | 'admin'>('client'); useEffect(() => { const savedCredentials = sessionStorage.getItem('camOS_credentials'); if (savedCredentials) { try { const { username, password, orgId } = JSON.parse(savedCredentials); const resolvedOrgId = orgId ?? determineOrgId({ username }); setCredentials({ username, password, orgId: resolvedOrgId }); setUserRole(username === 'admin' ? 'admin' : 'client'); setIsLoggedIn(true); } catch (error) { console.error('Failed to restore session:', error); sessionStorage.removeItem('camOS_credentials'); } } }, []); const handleLogin = (nextCreds: Credentials) => { const resolvedOrgId = nextCreds.orgId ?? determineOrgId({ username: nextCreds.username }); setCredentials({ ...nextCreds, orgId: resolvedOrgId }); setUserRole(nextCreds.username === 'admin' ? 'admin' : 'client'); setIsLoggedIn(true); sessionStorage.setItem( 'camOS_credentials', JSON.stringify({ ...nextCreds, orgId: resolvedOrgId }) ); }; const handleLogout = () => { sessionStorage.removeItem('camOS_credentials'); const hasViewToken = new URLSearchParams(window.location.search).has('view_token'); if (hasViewToken) { window.close(); setTimeout(() => { window.location.href = '/'; }, 100); } else { setIsLoggedIn(false); setCredentials({ username: '', password: '', orgId: undefined }); setUserRole('client'); } }; const hasViewToken = new URLSearchParams(window.location.search).has('view_token'); return ( <Router><GlobalControlsProvider><Routes> {} <Route path="/" element={!isLoggedIn && !hasViewToken ? <LandingPage /> : <Navigate to={userRole === 'admin' ? '/admin' : '/dashboard'} replace />} /><Route path="/login" element={!isLoggedIn && !hasViewToken ? <Login onLogin={handleLogin} /> : <Navigate to={userRole === 'admin' ? '/admin' : '/dashboard'} replace />} /> {} {(isLoggedIn || hasViewToken) && ( <><Route path="/dashboard" element={ <VRMLayout userRole={hasViewToken ? 'client' : userRole} onLogout={handleLogout}> {userRole === 'admin' && !hasViewToken ? ( <Navigate to="/admin" replace /> ) : ( <DashboardV2Page credentials={credentials} /> )} </VRMLayout> } /><Route path="/event-logs" element={ <VRMLayout userRole={hasViewToken ? 'client' : userRole} onLogout={handleLogout}> {userRole === 'admin' && !hasViewToken ? <Navigate to="/admin" replace /> : <EventLogsPage credentials={credentials} /> } </VRMLayout> } /><Route path="/alarm-logs" element={ <VRMLayout userRole={hasViewToken ? 'client' : userRole} onLogout={handleLogout}> {userRole === 'admin' && !hasViewToken ? <Navigate to="/admin" replace /> : <AlarmLogsPage credentials={credentials} /> } </VRMLayout> } /><Route path="/device-list" element={ <VRMLayout userRole={hasViewToken ? 'client' : userRole} onLogout={handleLogout}> {userRole === 'admin' && !hasViewToken ? <Navigate to="/admin" replace /> : <DeviceListPage credentials={credentials} /> } </VRMLayout> } /> {(() => { const analyticsElement = ( <VRMLayout userRole={hasViewToken ? 'client' : userRole} onLogout={handleLogout}><AnalyticsComingSoonPage /></VRMLayout> ); return ( <><Route path="/analytics" element={analyticsElement} /><Route path="/analytics/v2" element={analyticsElement} /><Route path="/analytics/legacy" element={analyticsElement} /><Route path="/analytics/*" element={analyticsElement} /></> ); })()} <Route path="/reports" element={ <VRMLayout userRole={hasViewToken ? 'client' : userRole} onLogout={handleLogout}> {userRole === 'admin' && !hasViewToken ? <Navigate to="/admin" replace /> : <ReportsPage credentials={credentials} /> } </VRMLayout> } /> {userRole === 'admin' && ( <Route path="/admin" element={ <VRMLayout userRole={userRole} onLogout={handleLogout}><AdminPage credentials={credentials} /></VRMLayout> } /> )} </> )} {} <Route path="*" element={ !isLoggedIn && !hasViewToken ? <Navigate to="/" replace /> : <Navigate to={userRole === 'admin' ? '/admin' : '/dashboard'} replace /> } /></Routes></GlobalControlsProvider></Router> ); }; export default App;
