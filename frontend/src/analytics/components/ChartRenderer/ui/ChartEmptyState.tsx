interface ChartEmptyStateProps {
  height: number;
  className?: string;
}

export const ChartEmptyState = ({
  height,
  className,
}: ChartEmptyStateProps) => {
  void height;
  void className;
  return null;
};
