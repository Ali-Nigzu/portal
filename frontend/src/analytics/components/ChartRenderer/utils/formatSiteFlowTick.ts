const toDate = (value: string): Date | null => { const parsed = new Date(value); return Number.isNaN(parsed.getTime()) ? null : parsed; }; const formatHour = (date: Date, withMinutes: boolean): string => { const hour = date.getHours().toString().padStart(2, "0"); return withMinutes ? `${hour}:00` : hour; }; const formatWeekday = (date: Date): string => date.toLocaleDateString("en-US", { weekday: "short" }); const formatMonthDay = (date: Date): string => date.toLocaleDateString("en-US", { month: "short", day: "2-digit" }); const formatWeekLabel = (date: Date): string => `Wk of ${formatMonthDay(date)}`; const formatMonth = (date: Date, withYear = false): string => date.toLocaleDateString("en-US", { month: "short", year: withYear ? "numeric" : undefined }); export const formatSiteFlowTick = ( timeframe: string | undefined, bucket: string | undefined, label: string, ): string => { const parsed = toDate(label); if (!parsed) { return label; } switch (timeframe) { case "today": case "yesterday": return formatHour(parsed, false); case "last_week": return formatWeekday(parsed); case "last_month": return bucket === "WEEK" ? formatWeekLabel(parsed) : String(parsed.getDate()); case "last_quarter": return formatWeekLabel(parsed); case "last_year": return formatMonth(parsed, false); case "all_time": return String(parsed.getFullYear()); default: return label; } }; export const formatSiteFlowTooltip = ( timeframe: string | undefined, bucket: string | undefined, label: string, ): string => { const parsed = toDate(label); if (!parsed) { return label; } switch (timeframe) { case "today": case "yesterday": return formatHour(parsed, true); case "last_week": return formatWeekday(parsed); case "last_month": return bucket === "WEEK" ? formatWeekLabel(parsed) : String(parsed.getDate()); case "last_quarter": return formatWeekLabel(parsed); case "last_year": return formatMonth(parsed, false); case "all_time": return bucket === "MONTH" ? formatMonth(parsed, true) : String(parsed.getFullYear()); default: return label; } };
