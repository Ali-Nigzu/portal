const getApiBaseUrl = (): string => { const envUrl = process.env.REACT_APP_API_URL; if (envUrl) { return envUrl; } if (process.env.REACT_APP_ENVIRONMENT === 'production') { return window.location.origin; } if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1') { return 'http://localhost:8000'; } return ''; }; export const API_BASE_URL = getApiBaseUrl(); export const API_ENDPOINTS = { SEARCH_EVENTS: `${API_BASE_URL}/api/search-events`, } as const;
